<table class="table table-striped table-hover">
  <thead>
  <tr>
    <th scope="col">email</th>
    <th scope="col">status</th>
    <th scope="col">quantity</th>
    <th scope="col">amount</th>
    <th scope="col">created_at</th>
    <th scope="col">updated_at</th>
  </tr>
  </thead>
  <tbody>
  <% @orders.each do |order| %>
    <tr>
      <td scope="row"><%= order.user&.email %></td>
      <td><%= order.status %></td>
      <td><%= order.lists.size %></td>
      <td><%= order.amount %></td>
      <td><%= order.created_at %></td>
      <td><%= order.updated_at %></td>
      <td><%= link_to :update_tracker, shipment_admin_order_path(order) %></td>
      <!--      <td><%#= link_to :inventory, inbound_admin_product_path(p), method: :get %></td>-->
      <!--      <td><%#= link_to :edit, edit_admin_product_path(p), method: :get %></td>-->
      <!--      <td><%#= link_to :delete, admin_product_path(p), method: :delete, data: { confirm: "Are you sure?" } %></td>-->
    </tr>
  <% end %>
  </tbody>
</table>